\begin{table}[h]
\centering
\caption{\textbf{State-of-the-art comparison} (beyond \xltrack{} scale). We compare our final model with other 7--8B parameter models. \hqpool yields a model that outperforms models trained on open datasets
and is competitive with models trained on private datasets.}
\label{table:pre-main-results}
\include{tables/sota_comparison_tabular}
%
\end{table}